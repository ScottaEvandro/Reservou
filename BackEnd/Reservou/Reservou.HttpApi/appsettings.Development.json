{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Postgres": "teste"
  }
}
/*
public static IServiceCollection AddVersioning(this IServiceCollection services)
    {
        services.AddApiVersioning(config =>
        {
            config.DefaultApiVersion = new ApiVersion(1, 0);
            config.AssumeDefaultVersionWhenUnspecified = true;
            config.ReportApiVersions = true;
        });

        return services;
    }

public static IServiceCollection AddCorsDefinition(this IServiceCollection services)
    {
        services.AddCors(o =>
            o.AddPolicy(
                "default",
                builder =>
                {
                    builder.AllowAnyOrigin().AllowAnyMethod().AllowAnyHeader();
                }
            )
        );

        return services;
    }

public abstract class BaseConnection
{
    protected string ConnectionString { get; private set; }
    protected int MaxAttempts { get; private set; }

    public BaseConnection(IConfiguration configuration)
    {
        ConnectionString = configuration.GetConnectionString("Postgresql_google");
        MaxAttempts = int.Parse(configuration.GetSection("MaxAttempts").Value);
    }
}
na classe usar nomeDaClasse : BaseConnection
no construtor construtor : base(configuration)
antes da query usar assim : using NpgsqlConnection connection = new(ConnectionString);
